<%= link_to 'Websites List', contact_info_admin_websites_path, class: "small button round" %>

<h1>New Website</h1>

<%if @contact.present?%>
<p>This new website will be associated with <%=link_to "#{@contact.name} - #{@contact.title}", edit_contact_info_admin_contact_path(@contact)%>. (You can change this later)<br />
Or if you want to create the website without associating it to a contact? <%= link_to "Click to here create new website without association.", new_contact_info_admin_website_path %>
</p>
<%end%>

<%= simple_form_for @website, url: contact_info_admin_websites_path do |f| %>
  <%= f.input :url, label: "URL" %>
  <%= f.input :label, label: "Label" %>
  <%= f.hidden_field :contact_id, value: "#{params[:website][:contact_id]}" unless params[:website].nil? %>

    </br>
  <%= f.button :submit %>
<% end # simple_form_for @website, url: contact_info_admin_websites_path do |f|%>
