<% called_from ||= 'contact' %>
<li id="contact_phone_<%= association.id %>">
	<% if (called_from == 'contact') %><span class="handle"><%= image_tag("icons/drag_handle.gif") %></span><% end %>
	
	<% if (called_from == 'contact') %> 
		<%= link_to("#{association.phone.phone} #{association.phone.label}", edit_contact_info_admin_phone_path(association.phone)) %>
	<% else %>
	  <%=link_to("#{association.contact.name}", edit_contact_info_admin_contact_path(association.contact))%> <span style="font-size: x-small"> - <%="#{association.contact.title}" %></span> 
	<% end %>	  
	
	<%= link_to(image_tag("icons/trash.gif"), 
			contact_info_admin_contact_phone_path(association, called_from: called_from), 
			confirm: "Are you sure? (This won't delete the phone --it will only remove the association.)", 
			remote: true,
			method: :delete) %>
</li>