<h2>Edit Account</h2>

<%= simple_form_for(resource, as: resource_name, url: user_registration_path, html: { method: :patch }) do |f| %>
  <%= f.error_notification %>
  <%= devise_error_messages! %>

  <div class="form-inputs">
    <%= f.input :email, required: true, autofocus: true %>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
    <% end %>

    <p>Yeah, there's not a lot here. The best way to change your password is to make sure your
    email address above is correct, then log out and follow the "Forgot My Password" process.</p>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Update" %>
  </div>
<% end %>

